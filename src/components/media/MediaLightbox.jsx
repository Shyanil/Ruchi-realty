import { useEffect } from "react";
export default function MediaLightbox({ items, index, onClose, onChange }) { const item=items[index]; useEffect(()=>{const key=(e)=>{if(e.key==="Escape")onClose();if(e.key==="ArrowRight")onChange((index+1)%items.length);if(e.key==="ArrowLeft")onChange((index-1+items.length)%items.length)};addEventListener("keydown",key);document.body.style.overflow="hidden";return()=>{removeEventListener("keydown",key);document.body.style.overflow=""}},[index,items.length]); if(!item)return null; return <div className="media-lightbox" role="dialog" aria-modal="true" aria-label={item.title} onClick={onClose}><button className="media-lightbox__close" onClick={onClose} aria-label="Close">×</button><button className="media-lightbox__prev" onClick={(e)=>{e.stopPropagation();onChange((index-1+items.length)%items.length)}} aria-label="Previous">‹</button><figure onClick={(e)=>e.stopPropagation()}><img src={item.image_url} alt={item.alt_text}/><figcaption><strong>{item.title}</strong>{item.caption && <span>{item.caption}</span>}<small>{index+1} / {items.length}</small></figcaption></figure><button className="media-lightbox__next" onClick={(e)=>{e.stopPropagation();onChange((index+1)%items.length)}} aria-label="Next">›</button></div>; }
