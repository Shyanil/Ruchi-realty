import { useEffect,useMemo,useState } from "react"; import Nav from "../components/Nav"; import { Footer } from "../components/Footer"; import SEO from "../components/SEO"; import MediaHero from "../components/media/MediaHero"; import { EVENT_TYPES,getEvents } from "../services/mediaService";
export default function EventsAwardsPage(){const[items,setItems]=useState([]),[type,setType]=useState("All");useEffect(()=>{getEvents().then(setItems)},[]);const filtered=useMemo(()=>items.filter(i=>type==="All"||i.item_type===type),[items,type]);const featured=items.find(i=>i.is_featured)||items[0],isAward=i=>["Award","Recognition"].includes(i?.item_type);return <><SEO title="Events & Awards | Ruchi Realty" description="Explore Ruchi Realty’s events, awards, recognitions, milestones, and media moments." canonical="https://ruchirealty.com/media/events-awards" image={featured?.image_url||"/assets/media/gallery/award-recognition-1.webp"}/><Nav/><main><MediaHero title="Events & Awards" breadcrumb="Events & Awards" subtitle="Events, recognitions, milestones, and the moments that mark our journey across eastern and central India."/><section className="events-awards section-pad"><div className="rr-wrap"><div className="event-filters">{EVENT_TYPES.map(item=><button className={item===type?"is-active":""} onClick={()=>setType(item)} key={item}>{item}</button>)}</div>{featured&&type==="All"&&<article className={`event-featured ${isAward(featured)?"is-award":""}`}><div className="event-featured__media"><img loading="eager" src={featured.image_url} alt={featured.title} fetchpriority="high" decoding="async"/></div><div><span>{featured.item_type}</span><h2>{featured.title}</h2><p>{featured.excerpt}</p>{featured.location&&<small>{featured.location}</small>}{featured.video_url&&<a href={featured.video_url} target="_blank" rel="noreferrer">Play video ▶</a>}</div></article>}<div className="event-timeline">{filtered.filter(i=>type!=="All"||i.id!==featured?.id).map((item,index)=><article className={`event-card ${isAward(item)?"is-award":""}`} key={item.id}><span className="event-card__line">{String(index+1).padStart(2,"0")}</span><div className="event-card__image">{item.image_url?<img src={isAward(item)?item.image_url:(item.thumbnail_url||item.image_url)} alt={item.title} loading="lazy" decoding="async"/>:<div className="media-placeholder">RR</div>}{item.video_url&&<a href={item.video_url} target="_blank" rel="noreferrer" aria-label={`Play ${item.title}`}>▶</a>}</div><div><small>{item.item_type}{item.event_date?` · ${new Date(item.event_date).getFullYear()}`:""}</small><h3>{item.title}</h3><p>{item.excerpt}</p>{item.location&&<em>{item.location}</em>}{item.external_url&&<a href={item.external_url} target="_blank" rel="noreferrer">Learn more →</a>}</div></article>)}</div>{!filtered.length&&<div className="media-empty"><strong>Events & Awards will be updated soon.</strong></div>}</div></section></main><Footer/></>}
